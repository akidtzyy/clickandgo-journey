---
trigger: always_on
---

# Antigravity Rules
- Focus ONLY on the files explicitly specified or currently open in the active tab.
- Do NOT run background terminal commands (e.g. artisan, npm) automatically unless requested.
- Skip scanning node_modules, backend/vendor, and backend/storage folders.